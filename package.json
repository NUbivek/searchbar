{
  "name": "searchbar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "rm -rf .next out && next build && cp CNAME out/",
    "start": "next start"
  }
}
