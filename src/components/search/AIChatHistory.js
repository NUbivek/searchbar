import React from 'react';

// This component displays the chat history between the user and AI
const AIChatHistory = () => {
  // Always return null to hide the chat history
  return null;
};

export default AIChatHistory; 